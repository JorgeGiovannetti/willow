
class Vector {
    + x: int;
    + y: int;
    + z: int;
}

fn main() {
    v: Vector;

    v.x = 5;
    v.y = 4;
    v.z = 3;

    sumOfComponents: int = v.x + v.y + v.z;

    write("Sum of components: ");
    writeln(sumOfComponents);
}